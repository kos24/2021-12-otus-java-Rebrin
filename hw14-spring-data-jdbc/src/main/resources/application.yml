server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/homeworkDB
    username: usr
    password: pwd
  flyway:
    enabled: true

logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE
